<div class="col-6 center">
  <h1>Editing Switch Destination</h1>

  <%= render 'form', switch_destination: @switch_destination %>

  <%= link_to 'Show', switch_switch_destination_path([@switch_destination.switch, @switch_destination]) %> |
  <%= link_to 'Back', switch_switch_destinations_path(@switch_destination.switch) %>
</div>
