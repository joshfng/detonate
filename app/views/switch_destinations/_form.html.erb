<%= form_with(model: switch_destination, url: [switch_destination.switch, switch_destination]) do |form| %>
  <% if switch_destination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(switch_destination.errors.count, "error") %> prohibited this switch_destination from being saved:</h2>

      <ul>
        <% switch_destination.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :switch_destination_type %>
    <%= form.text_field :switch_destination_type, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :switch_destination_address %>
    <%= form.text_field :switch_destination_address, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Create Switch Destination', class: 'btn btn-primary' %>
  </div>
<% end %>
