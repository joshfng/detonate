<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @switch.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= @switch.content %>
</p>

<p>
  <strong>Heartbeat interval:</strong>
  <%= @switch.heartbeat_interval %>
</p>

<p>
  <strong>Max missed heartbeats:</strong>
  <%= @switch.max_missed_heartbeats %>
</p>

<p>
  <strong>Missed heartbeat:</strong>
  <%= @switch.missed_heartbeats %>
</p>

<%= link_to 'Edit', edit_switch_path(@switch) %><br><br>
<%= link_to 'Heartbeat Destinations', switch_heartbeat_destinations_path(@switch) %><br>
<%= link_to 'Switch Destinations', switch_switch_destinations_path(@switch) %><br><br>
<%= link_to 'New Heartbeat Destination', new_switch_heartbeat_destination_path(@switch) %><br>
<%= link_to 'New Switch Destination', new_switch_switch_destination_path(@switch) %><br><br>
<%= link_to 'Back', switches_path %>
